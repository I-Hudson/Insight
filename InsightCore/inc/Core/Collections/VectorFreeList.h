#pragma once

namespace Insight
{
	namespace Core
	{
		/// <summary>
		/// Store items within a vector, but don't allow items to be moved 
		/// when things are removed. Don't allow shifting of items.
		/// </summary>
		/// <typeparam name="T"></typeparam>
		template<typename T>
		class VectorFreeList
		{

		};
	}
}